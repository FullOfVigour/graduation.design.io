import Vuex from 'vuex'
import base from './base'
// import baseModules from '../modules/index'

// const { feature, layout, backups, analysis } = baseModules

base.modules = {
  // feature,
  // layout,
  // backups,
  // analysis
}

export default new Vuex.Store(base)
